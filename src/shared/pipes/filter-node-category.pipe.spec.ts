import { FilterNodeCategoryPipe } from './filter-node-category.pipe';

describe('FilterNodeCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNodeCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
